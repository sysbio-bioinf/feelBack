import { InstrumentAssembler } from './instrument.assembler';

describe('InstrumentAssembler', () => {
  it('should be defined', () => {
    expect(new InstrumentAssembler()).toBeDefined();
  });
});
