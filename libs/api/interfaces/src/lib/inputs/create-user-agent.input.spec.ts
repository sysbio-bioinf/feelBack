import { CreateUserAgentInput } from './create-user-agent.input';

describe('CreateUserAgentInput', () => {
  it('should be defined', () => {
    expect(new CreateUserAgentInput()).toBeDefined();
  });
});
