import { UpdateInstrumentInput } from './update-instrument.input';

describe('UpdateInstrumentInput', () => {
  it('should be defined', () => {
    expect(new UpdateInstrumentInput()).toBeDefined();
  });
});
