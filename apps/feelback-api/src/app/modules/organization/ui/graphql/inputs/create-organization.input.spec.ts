import { CreateOrganizationInput } from './create-organization.input';

describe('CreateOrganizationInput', () => {
  it('should be defined', () => {
    expect(new CreateOrganizationInput()).toBeDefined();
  });
});
