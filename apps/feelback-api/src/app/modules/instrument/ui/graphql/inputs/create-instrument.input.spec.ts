import { CreateInstrumentInput } from './create-instrument.input';

describe('CreateInstrumentInput', () => {
  it('should be defined', () => {
    expect(new CreateInstrumentInput()).toBeDefined();
  });
});
