import { CreateIdentityInput } from './create-identity.input';

describe('CreateIdentityInput', () => {
  it('should be defined', () => {
    expect(new CreateIdentityInput()).toBeDefined();
  });
});
